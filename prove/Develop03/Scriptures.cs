// list of Word objects

public class Scripture {
    public List<Word> Words = new List<Word>();

    // constructor that takes argument "text" and separates by spaces, then add word and index to list of words. This uses the Word class constructor.

    // method to hide certain words
}